---
description: Phase 3 — call stacks, file tree, and signatures
argument-hint: [docs/plans/<slug>]
---

Use the `plan-program` skill.

Feature directory: $ARGUMENTS

Verify `02-architecture.md` is `status: approved` before starting. Typecheck
the signature stubs before presenting them — at the checkpoint, not only at the
gate — and report the result at the gate.

Draw before you write. The call stacks, the file tree, the signatures, and the
test plan are each settled in the conversation first — drawn in full, one
elicitation each, stop — and only then assembled into `03-program-design.md`.

Stop at the gate, and ask whatever the checkpoints left open as elicited options
with their consequences named. Do not continue to slices.
